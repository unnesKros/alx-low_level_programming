#include "main.h"


char *_strncat(char *dest, char *src, int n)
{
	int i, j;

	for (i =0; dest[i] != '\0'; i++)
		;
	n = i + n;
	for (i = i, j = 0; i < n; i++, j++)
	{
		dest[i] = src[j];
	}


	dest[i] = '\0';

	return dest;
}
